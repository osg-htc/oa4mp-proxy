services:
  nginx:
    build:
      dockerfile: test.Dockerfile
    ports:
      - "8081:8081"
    # Uncomment below if you want to mount nginx.conf from host
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf:ro
